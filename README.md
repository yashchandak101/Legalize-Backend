# FeedZen
FeedZen is a deterministic policy-driven content moderation system that filters feed content based on user-defined preferences.  The system is intentionally designed to separate AI classification (probabilistic) from policy enforcement (deterministic) to ensure correctness, resilience, and safety as the system evolves.
